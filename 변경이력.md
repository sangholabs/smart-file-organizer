# 변경 이력

## v2.1.1 (2026-06-09) — 문서: SAC/MotW 차단 해결 가이드
- 새 PC에서 GitHub zip 의 `.bat`/`.vbs` 가 **Smart App Control 에 차단**되는 원인이 exe 가 아니라 **Mark‑of‑the‑Web(인터넷에서 받음 표식)** 임을 명시.
- 해결법 문서화(`README.md`·`사용법.txt`): ① zip 우클릭→속성→**"차단 해제"** 후 압축 해제, ② PowerShell `Unblock-File`, ③ 터미널 `py app.py`, ④ `git clone`(표식 없음). **GitHub Releases 로는 SAC 우회 불가**임을 정정.
- 코드 변경 없음(문서 전용).

## v2.1.0 (2026-06-08) — 임계값 튜닝 + 환경점검 + 자동설치
- **임계값 캘리브레이션:** ffmpeg/합성 픽스처로 동일군 vs 상이군 분포 측정(`tools/calibrate.py`, 개발용·exe 미포함).
  결과: 영상 동일 ≤4 / 무관 ≥155 → `video_threshold=40` 확정. 근접문서 소편집1.0·중0.55·대0.02·무관0 → `text_near_threshold` **0.85→0.5**(중편집까지 포착). 오디오는 동일/무관 분포가 겹쳐 신뢰도 낮음 확인 → 기본 OFF·`audio_threshold=10` 유지. 측정·근거·조절법은 `튜닝_결과.md`.
- **🩺 환경 점검(doctor):** `py main.py doctor` 로 선택 기능 가용성과 빠진 라이브러리 설치법을 한눈에. GUI 요약 탭 하단에도 "환경 점검: …" 한 줄(클릭 시 상세). 다른 PC에서 무엇이 빠졌는지 즉시 파악.
- **자동 설치(`설치.bat`):** 새 PC에서 더블클릭 → Python 감지(없으면 winget으로 자동 설치) → `requirements.txt` 설치 → 환경 점검까지. ASCII 전용.
- **리포트 요약 확장:** CLI `report` 요약 줄에 이미지/문서/근접문서/영상/오디오/압축/풀린압축/찌꺼기/빈폴더 건수 추가(기존 3종 → 전 카테고리).
- **소스 배포:** `배포_소스.zip`(폴더 복사 + 설치.bat 시나리오용, dist/data/__pycache__ 제외). 코드 위생: 미사용 `analyzer._load_rows` 제거.
- 신규 외부 의존성 없음. 자동 테스트 **62개**(doctor 2개 추가).

## v2.0.0 (2026-06-07) — 품질·안정성·확장(감사 #4)
- **GUI 토글:** 1단계 옵션에 OCR·영상·오디오(실험적) 켜고/끄기 추가(이전엔 config.json만).
- **무거운 작업 분리:** 영상·오디오·RAW·OCR를 별도 소규모 풀(`media_workers`)로 → CPU/메모리 폭주 방지. 가벼운 지문은 기존 병렬 풀.
- **OCR 크기 가드:** 아이콘/썸네일(긴 변 200px 미만) 건너뛰고, 거대한 이미지는 축소 후 OCR.
- **HTML 리포트 일관성:** CLI `report` HTML 에 이미지·영상·오디오·문서·근접문서·압축·풀린압축·찌꺼기·빈폴더 전부 반영(기존 3종만 → 전 카테고리).
- **🎵 비슷한 오디오(실험적·기본 OFF):** 전체 평균 스펙트럼 지문. 정확도 한계로 기본 꺼짐, GUI 토글로 시도.
- **이미지 정밀도 옵션:** 회전/반전 매칭(`image_rotation`, 기본 OFF·실험적), pHash(`image_hash="phash"`, DCT 기반).
- **대용량 메모리 최적화:** analyzer 가 전체 목록을 메모리에 올리지 않고 **중복 후보만 SQL로 조회**(완전중복=해시그룹, 이름충돌=동명그룹, 버전=가족 2-pass). 결과 동일(60개 테스트로 고정).
- 신규 외부 의존성 없음(ffmpeg·numpy 기존). 자동 테스트 60개.

## v1.9.0 (2026-06-07) — 무거운 미디어 + 추가 보강(감사 #3)
- **🎬 비슷한 영상:** 재인코딩·해상도 다른 같은 영상을 키프레임 dHash(imageio-ffmpeg)로 탐지.
- **OCR(스캔 PDF + 이미지, 한+영):** 글자 없는 PDF/이미지를 rapidocr+PyMuPDF 로 글자 추출 → 같은/비슷한 문서에 합류(스캔본↔원본 매칭).
- **🟪 RAW 사진(.dng/.cr2 등):** rawpy 썸네일 dHash → 비슷한 이미지에 합류.
- **🗂 풀린 압축:** zip/7z 내용이 이미 디스크에 다 풀려있으면 압축파일을 정리 후보로(검토).
- **📊 요약 대시보드(첫 탭):** 카테고리별 건수 카드(클릭→이동). 느슨 이미지 클러스터링 BK-tree 로 가속.
- 새 의존성(imageio-ffmpeg/rapidocr/PyMuPDF/rawpy/numpy)은 **선택/그레이스풀** — 없으면 해당 탐지만 생략. OCR/영상은 느려 config 로 끌 수 있음(`ocr_enabled`/`video_match`). 자동 테스트 56개.
- ⚠️ 이 기능들 때문에 exe 가 매우 커짐. exe 번들 실패 라이브러리는 소스(.vbs) 실행 시에만 동작.

## v1.8.0 (2026-06-06) — 탐지 정밀화 2차(감사 #2)
- **지문 계산 병렬화:** 이미지 dHash·문서 텍스트·압축 매니페스트 계산을 멀티코어로 → 사진·문서 많은 드라이브 스캔 속도 향상(결과 동일).
- **🟫 비슷한 문서(근접 중복):** 약간 수정된 문서도 MinHash+LSH 유사도(기본 0.85)로 묶음. 정확 동일은 기존 "같은 내용 문서"로 분리. 기본 미체크(검토).
- **스마트 보관본 선택:** 중복 중 남길 1개를 다운로드/임시/`복사본`/`(1)` 회피하고 **원본 우선**으로 선택(완전중복·이미지·문서·압축 공통).
- **7z 내용중복:** `.7z`도 zip 과 동일한 (이름·크기·CRC) 매니페스트로 비교 → `.7z↔.zip` 교차 중복까지(py7zr).
- 새 의존성(py7zr)·MinHash 모두 선택/그레이스풀. catalog.db 는 `txtmin` 컬럼 자동 마이그레이션. 자동 테스트 53개(전부 그린).

## v1.7.0 (2026-06-06) — 탐지 전면 보강(감사 반영)
- **작은 파일 사각지대 해소:** 기본 `min_size_bytes` 1MB→100KB + GUI "최소 크기" 콤보(0/100KB/500KB/1MB/5MB). 그동안 1MB 미만이라 안 잡히던 작은 중복까지 탐지.
- **이미지 확장:** `.heic/.heif/.avif/.jfif/.ico` 추가, **HEIC(아이폰 사진) 시각중복** 지원(pillow-heif).
- **아카이브 확장:** `.jar/.war/.apk/.epub`(zip 포맷)도 내용중복 비교. (7z/rar은 범위 밖)
- **문서 확장:** docx **머리글·바닥글·각주까지** 텍스트 비교, `.odt/.ods/.odp`(오픈오피스)·`.hwpx`(한글) 지원, 구형 `.hwp`는 olefile 프리뷰 텍스트로 근사 비교.
- **📂 빈 폴더 정리:** 완전 빈 폴더 + '찌꺼기만 남은 폴더'를 새 탭에서 정리(폴더째 격리 이동·복원). 중첩 시 최상위 빈 폴더만.
- **정확도:** 하드링크(같은 실체)는 한 번만 색인(가능한 OS에서). 중복 설정(Thumbs.db) 정리.
- 새 의존성(pillow-heif·olefile)은 **선택** — 없으면 해당 탐지만 자동 생략. 자동 테스트 50개로 확대(전부 그린).

## v1.6.0 (2026-06-06)
- **🧹 시스템 찌꺼기 정리 추가:** 맥 작업 시 생기는 `._파일`(AppleDouble)·`.DS_Store`,
  윈도우 `Thumbs.db` 등 숨김 찌꺼기를 **크기 제한과 무관하게** 찾아 새 탭에 모음.
  삭제 안전하므로 **기본 체크**, 정리는 격리폴더 이동(되돌리기 가능). 패턴은 `config.json` 의 `junk_globs` 로 편집.
- 이 찌꺼기들은 대개 1MB 미만이라 기존엔 스캔조차 안 됐던 것을 별도 수집 경로로 해결.
- 자동 테스트 2개 추가(크기/제외 필터 우회 탐지, 격리 이동·복원). 총 45개 그린.

## v1.5.0 (2026-06-06) — 전체 고도화
- **성능:** 중복 해시를 **멀티코어 병렬(ThreadPoolExecutor)**로 계산 → 대용량·다중 드라이브 스캔 속도 대폭 향상. 결과는 동일(43개 테스트로 검증). `config.json` 의 `hash_workers` 로 조절.
- **인사이트:** **📊 용량 분석 탭** 추가 — 용량 많이 쓰는 폴더(하위 포함), 가장 큰 파일, 오래된 대용량 파일(2년+ 미수정), 확장자별 용량. 더블클릭으로 파일/폴더 열기.
- **자동화:** **🗑 격리 비우기** 버튼(보관본을 휴지통으로, 복원 가능) + `quarantine_purge_days` 로 오래된 격리본 시작 시 자동 정리 + 스캔 이력 기록(`data/scan_history.json`).
- **탐지:** 이미지 **느슨 매칭 옵션**(`image_match=loose`, 해밍거리 임계값) — 약간 다른(보정·크롭) 사진도 묶기. GUI 체크박스로 켜면 **재스캔 없이 즉시 재분석**.
- 자동 테스트 4개 추가(`tests/test_advanced.py`): 병렬 해시 정확성·인사이트·격리 비우기·이미지 클러스터링.
- **다른 PC 배포:** 자체완결형 `배포_파일정리.zip`(exe + 사용법.txt) — 파이썬 설치 없이 더블클릭 실행.
  설정 파일을 다른 PC로 옮겨도 검사 폴더가 전부 없으면 **그 PC 기준으로 자동 재설정**(사용자 토글은 유지)되어 복사 후 바로 사용 가능.

## v1.4.0 (2026-06-06)
- **UX 대폭 개선:**
  - **스캔 진행표시 + 멈춤**: 진행바 + "수집/해시/지문 N개·현재 항목" 표시, 스캔 버튼이 **■ 멈춤**으로 전환(취소 시 부분 색인 보존, 다음 스캔에 이어짐).
  - **결과 한눈에 보기**: 상단 6색 요약 칩(건수 표시, 클릭 시 해당 탭 이동) + 회수가능 칩. **결과 0인 탭은 자동 흐림(비활성)**, 스캔 후 결과 많은 탭 자동 선택.
  - **검색 · 필터**: 파일명/경로 검색 + "크기 ≥"(1MB/10MB/100MB/1GB) 필터.
  - **키보드 · 편의**: F5=스캔/멈춤, Esc=멈춤, Space=체크 토글, Enter=열기, Ctrl+A=현재 탭 모두 선택, 컬럼 머리글 클릭=정렬, **창 크기·위치 기억**(다음 실행 복원).
- scanner/fingerprint 에 선택적 `progress`/`cancel` 훅 추가(기본값 None → 기존 테스트·CLI 영향 없음).

## v1.3.0 (2026-06-06)
- **새 탐지 3종 추가(정규화된 "내용" 기준 — 바이트가 달라도 사실상 같은 파일):**
  - 🟪 **비슷한 이미지**: 리사이즈·재압축·메타데이터만 다른 사실상 같은 사진을 dHash(시각 지문)로 탐지. (Pillow 필요)
  - 🟩 **같은 내용 문서**: docx·xlsx·pptx(표준 zipfile)·pdf(pypdf)·텍스트/코드에서 글자만 뽑아 비교(서식·메타데이터·인코딩·줄바꿈 차이 무시).
  - 📦 **같은 내용 압축**: 압축방식·날짜만 다른 같은 내용의 zip 을 내용 매니페스트로 탐지.
- GUI 에 탭 3개 추가(총 6탭). 새 카테고리는 키퍼 1개 보존 + 기본 미체크(검토).
- 지문은 `fingerprints` 캐시 테이블에 저장되어 재스캔 시 재사용(증분).
- 라이브러리(Pillow/pypdf)가 없으면 해당 탐지만 자동 생략, 핵심 기능은 그대로.
- 자동 테스트 8개 추가(`tests/test_fingerprint.py`).

## v1.2.1 (2026-06-06)
- **테스트 검증 추가:** `tests/test_organizer.py` 자동 테스트 31개(중복정확성/이름충돌/버전이상/스캐너/적용·undo·안전).
  `테스트.bat` 으로 실행. 결과·한계는 `테스트_검증_결과.md`.
- **버그 수정(테스트로 발견):**
  - 0바이트(빈) 파일이 전부 "완전중복"으로 묶이던 문제 → 빈 파일은 중복 대상에서 제외.
  - 버전 가족 이름이 부분 문자열로 과결합(예: 자료 ↔ 원본자료) → 토큰 경계 기준으로 분리.
  - 긴 경로(>260자) 격리 이동/복원 실패 가능성 → Windows 긴 경로(`\\?\`) 대응.
  - 일부 콘솔에서 오류 메시지의 특수문자(—, →) 출력이 깨지던 것 → ASCII로 교체.

## v1.2.0 (2026-05-31)
- **UI 재설계:** 직관성·가독성 개선.
  - `clam` 테마 + 큰 글씨(맑은 고딕) + 행 높이 확대 + 얼룩무늬(zebra) 배경.
  - 화면을 `1단계 폴더 선택 → 2단계 항목 선택 → 3단계 정리 실행` 단계형으로 재배치.
  - 결과를 **완전중복/이름충돌/버전이상 탭(건수 표시)**으로 분리.
  - **선택된 행은 배경색으로 강조**, 그룹 줄 클릭 시 그룹 전체 토글.
  - 하단에 `선택됨 N개 · 예상 회수 X` 실시간 카운터, 진행 로그는 접이식.
  - 옵션(휴지통/같은 드라이브)을 1단계 카드로 이동 + 툴팁.
- **추가/변경:** 클릭 동작 분리 — **맨 앞 "선택"(☑) 칸 클릭 = 체크/해제**,
  **파일명 등 다른 칸 클릭 = 파일 열기**(기본 프로그램). 그룹 줄 클릭 = 그룹 전체 토글.
  우클릭 = 파일 열기 / 폴더에서 보기. (더블클릭 시 체크가 깜빡이던 문제 제거)

## v1.1.0 (2026-05-31)
- **추가:** "외장정리: 같은 드라이브에 격리" 옵션 (`quarantine_per_drive`).
  외장/USB 정리 시 각 파일이 있던 드라이브 안에 격리폴더를 만들어 드라이브 간 복사를 방지.
- **추가:** "삭제 대신 휴지통으로 보내기" 토글을 GUI 에 노출 (`use_recycle_bin`).
  휴지통 모드에서는 "같은 드라이브에 격리" 옵션이 자동 비활성화됨.
- **추가:** `파일정리_실행.vbs`(깜빡임 없는 권장 런처) / `파일정리_실행.bat` —
  Smart App Control 이 켜진 PC 에서도 동작하도록 서명된 `pythonw.exe` 로 GUI 를 실행.
- **수정:** 런처 .bat 의 한글 주석/메시지가 cmd 파싱을 깨뜨려 GUI 가 안 뜨던 문제 →
  .bat 을 순수 ASCII 로 정리(한글 제거).
- **추가:** `안전가이드.md` (안전 수칙·외장장치 주의점), `변경이력.md`.
- **빌드:** exe 에 `send2trash` 포함(`--hidden-import`)하여 휴지통 모드가 exe 에서도 동작.
- **알림:** 이 PC 는 Smart App Control(enforced) 이라 서명 안 된 exe 는 실행 차단됨.
  → 평소엔 `파일정리_실행.bat` 사용. exe 는 SAC 꺼진 다른 PC 배포용.

## v1.0.0 (2026-05-31)
- 최초 버전.
- 스캔/해시 색인(2단계 해시: 크기→부분→전체), 증분 재스캔.
- 3가지 탐지: 완전중복 / 이름 같음·내용 다름 / 버전 이상.
- HTML 리포트(체크박스 + decisions.json 내보내기).
- 네이티브 tkinter GUI(`app.py`): 스캔·결과 트리·체크·미리보기·이동·되돌리기.
- 안전한 적용: 드라이런 기본, 격리폴더 이동, undo 로그/복원, 영구삭제 없음.
- PyInstaller 단독 exe 빌드, CLI(`main.py`) 및 CLI 메뉴(`정리도구.bat`).
