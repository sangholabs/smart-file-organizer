# 테스트 검증 결과

스캔·정리 과정에서 발생 가능한 이슈를 정리하고, 자동 테스트(`tests/test_organizer.py`)로 검증했습니다.
직접 돌려보려면 **`테스트.bat`** 더블클릭 (또는 `py tests/test_organizer.py`).

> 모든 테스트는 임시폴더 + 임시 색인을 사용하며 **실제 파일·설정을 건드리지 않습니다.**

## 결과 요약
- **자동 테스트 39개 전부 통과** (핵심 31 + 내용기반 8). 검증 중 발견된 실제 버그 3건은 수정 완료.
- 핵심 로직은 `tests/test_organizer.py`, 이미지/문서/zip 내용중복은 `tests/test_fingerprint.py`.

## 점검한 이슈와 검증 항목

### A. 중복 탐지 정확성 — "내용이 다른데 정리되는" 사고 방지 (가장 중요)
- 내용이 완전히 같은 파일만 중복으로 묶임(전체 해시 일치). 보관 1개는 항상 남김.
- **같은 크기지만 내용이 다른 경우 중복으로 묶지 않음** — 검증:
  - 200KB에서 앞/뒤 64KB는 같고 가운데 1바이트만 다른 파일 → 중복 아님
  - 64~128KB 구간에서 앞부분만 같고 뒤가 다른 파일 → 중복 아님
  - 작은 파일 내용 차이 → 중복 아님
- 사본 3개 이상: 보관본 선택(가장 짧은 경로) + 회수 용량 계산 정확.
- **0바이트(빈) 파일은 중복으로 묶지 않음** ← [수정] 예전엔 빈 파일끼리 전부 중복 처리되던 것 교정.

### B. 이름 같음·내용 다름
- 같은 이름·다른 내용 → 충돌로 표시(자동 정리 안 함, 검토 권장).
- 같은 이름·같은 내용 → 충돌 아님(완전중복으로만 처리).
- 대소문자 무시(Report.PDF = report.pdf) 정확.

### C. 버전 이상(휴리스틱)
- 이름은 "최종/v2"인데 수정일이 더 옛날 → 이상으로 표시(기본 미체크=검토, 자동 정리 안 됨).
- 버전과 수정일이 일관되면(정상) 표시 안 함.
- 무관한 이름은 한 가족으로 묶지 않음.
- **"자료"와 "원본자료"처럼 글자가 붙은 경우 잘못 묶지 않음** ← [수정] 토큰 경계 기준으로 분리.
- 내용이 동일한 버전들은 이상으로 보지 않음(손실 위험 없음).

### D. 스캐너 견고성
- 최소 크기 미만/제외 폴더(`__skip__`)/제외 확장자(`*.tmp`) 필터 동작.
- 증분 재스캔: 변경된 파일은 다시 해시, 삭제된 파일은 색인에서 제거.
- 한글·공백 파일명 정상 처리.
- **디렉터리 정션(reparse point)은 따라가지 않음**(무한루프·중복 색인 방지) — 실제 정션 생성해 검증.

### E. 적용 / 되돌리기 / 안전
- 드라이런은 파일을 옮기지 않음. 실제 이동 후 **되돌리기로 원위치 복원**.
- 없는 경로는 건너뜀, 중복 경로는 1회만 이동.
- **되돌리기는 원위치에 파일이 다시 생겼으면 덮어쓰지 않음**.
- **한 파일 이동이 실패해도 나머지는 정상 이동**(복원력) — 실패한 파일은 원위치 유지.
- 이름 충돌 시 자동 번호로 양쪽 보존(덮어쓰기 금지).
- 모든 사본을 옮겨도 **영구삭제가 아니라 격리폴더 보존**(복구 가능).
- 외장용 "같은 드라이브 격리" 경로 로직 정확.
- **소스 코드에 영구삭제 호출(os.remove/unlink/rmtree)이 없음**을 정적 검사로 보장.
- **긴 경로(>260자)도 격리 이동 성공** ← [수정] `\\?\` 긴 경로 대응 추가.

### F. 내용 기반 중복 — 이미지 / 문서 / zip (v1.3.0)
- **이미지 시각중복**: 같은 사진을 **리사이즈·재압축(jpg/png)·메타데이터만 다르게** 저장해도 한 그룹으로 탐지. 명백히 다른 사진은 안 묶음. 바이트까지 같은 사본은 "완전중복"으로만(중복 표시 안 함).
- **문서 내용중복**: 같은 본문에 **작성자 등 메타데이터만 다른 docx** → 한 그룹. 본문이 다르면 안 묶음. **인코딩만 다른 텍스트(UTF-8 vs UTF-16)** → 같은 내용으로 탐지.
- **zip 내용중복**: **압축방식(STORED vs DEFLATED)만 다른 같은 내용** zip → 한 그룹. 내용이 다르면 안 묶음. 그룹마다 보관본 1개 유지.

## 검증 중 수정한 버그
1. **0바이트 파일이 전부 "완전중복"으로 묶이던 문제** → 빈 파일은 중복 대상에서 제외.
2. **버전 가족 이름이 부분 문자열로 과도하게 묶이던 문제**(예: 자료 ↔ 원본자료) → 토큰 경계 기준 분리.
3. **긴 경로(>260) 이동 실패 가능성** → 이동·생성·복원 시 Windows 긴 경로(`\\?\`) 적용.
   (더불어 일부 콘솔에서 오류 메시지의 특수문자 출력이 깨지던 것도 ASCII로 교체)

---

## 알려진 한계 (설계상/환경상)
- **타임스탬프+크기 보존 변경**: 내용이 바뀌었는데 크기와 수정시간이 모두 그대로면 증분 스캔이 옛 해시를 재사용해 변경을 놓칠 수 있음. 의심되면 `data\catalog.db` 삭제 후 전체 재스캔.
- **버전 이상은 휴리스틱**: 특이한 파일명 규칙은 놓치거나 과탐 가능. 그래서 **기본 미체크(검토)**이며 자동 정리되지 않음 — 최종 판단은 사용자.
- **휴지통 모드**: 일부 외장/FAT/네트워크 드라이브엔 휴지통이 없어 보장되지 않음 → 외장은 격리폴더 모드 권장(`안전가이드.md`).
- **권한 거부/잠긴 파일/클라우드(offline) 파일**: 스캔 시 건너뛰고 로그에 남김(크래시 없음). 잠긴 파일은 이동 시 실패로 기록되고 나머지는 계속 진행.
- **드라이브 문자 변경**: 외장을 다시 꽂아 문자가 바뀌면 색인·되돌리기 기록이 안 맞음 → `안전가이드.md` 참고.
- **하드링크/심볼릭**: 심볼릭·정션은 따라가지 않음. 하드링크는 별개 파일로 보일 수 있음.
- **수동 decisions.json 편집**: 앱(GUI)은 완전중복에서 최소 1개 보관을 강제하지만, decisions.json을 직접 편집해 모든 사본을 옮기면 원위치엔 안 남음. 단 격리폴더에 보존되어 복구 가능(영구삭제 아님).
- **이미지/문서/zip 내용중복은 라이브러리 선택사항**: 이미지=Pillow, PDF=pypdf 가 없으면 해당 탐지만 자동 생략(핵심 기능은 그대로). docx/xlsx/pptx/zip·텍스트는 추가설치 없이 동작.
- **이미지 시각중복은 휴리스틱(dHash)**: 매우 단조로운 이미지(단색·빈 화면 등)는 서로 달라도 같은 지문이 될 수 있음. 그래서 **기본 미체크(검토)**이며 키퍼 1개는 항상 보존.
- **문서 텍스트중복은 "글자" 기준**: 표/이미지/도형만 있고 글자가 거의 없는 문서나, 스캔 PDF(글자 없는 이미지 PDF)는 비교 대상에서 빠질 수 있음.
