# 안전 가이드

이 도구가 **데이터를 잃지 않도록** 어떻게 동작하는지, 그리고 **외장/이동식 장치**를 정리할 때
무엇을 주의해야 하는지 정리한 문서입니다.

---

## 1. 기본 안전 원칙 (설계상 보장)

### 스캔/분석은 100% 읽기 전용
- 파일을 **열어서 읽기만** 합니다(해시 계산용). 분석 단계에는 파일을 수정·이동·삭제하는 코드가
  **존재하지 않습니다.** 어떤 드라이브를 스캔하든 원본은 그대로입니다.

### 정리(이동)는 영구 삭제가 아님
- 선택한 파일은 **격리폴더로 이동**하거나(되돌리기 가능), **Windows 휴지통**으로 보냅니다.
- 코드 어디에도 영구 삭제(`os.remove`/`delete`)는 없습니다. 오직 "이동"뿐입니다.

### 3단계 확인 흐름
1. **스캔/분석** → 결과 표시
2. **미리보기** → 무엇이 어디로 옮겨질지 먼저 확인
3. **실제 이동** → 확인창에서 동의해야 실행

### 추가 안전장치
- **완전중복은 최소 1개를 항상 보관** — 보관본은 체크 자체가 불가(🔒 표시).
- **파일별 오류 격리** — 잠긴 파일·권한 거부·장치 분리 등은 건너뛰고 로그만 남긴 뒤 계속 진행.
  한 파일의 실패가 전체 작업을 망치지 않습니다.
- **이동은 건당 처리** — 중간에 중단돼도 각 파일은 *원위치 아니면 격리폴더* 둘 중 하나에 온전히 존재(소실 없음).
- **심볼릭 링크/정션 건너뜀** — 무한 루프 방지.
- **클라우드 전용(offline) 파일 건너뜀** — OneDrive 등에서 원치 않는 다운로드 유발 방지.
- **되돌리기(undo)** — 격리폴더 모드의 모든 이동은 기록되어 원위치로 복원할 수 있습니다.

### 개인정보
- 모든 처리는 이 PC 안에서만 이루어집니다. 파일 내용·목록은 인터넷이나 외부로 **전송되지 않습니다.**

---

## 2. 외장 / 이동식 / 네트워크 장치 주의점

스캔은 어떤 장치든 안전하지만, **이동(정리)** 할 때 다음을 알아두세요.

### ① 격리폴더 위치 — "같은 드라이브에 격리" 옵션을 켜세요
- 기본 격리폴더는 C드라이브(바탕화면)에 있습니다. 외장 F:를 정리하면 파일이
  **드라이브를 건너 C:로 복사 후 삭제**되어 느리고 C: 용량을 씁니다.
- **해결:** GUI에서 **"외장정리: 같은 드라이브에 격리"** 를 켜면, 각 파일이 있던 드라이브 안
  (`F:\_정리보관\...`)으로 이동해 드라이브 간 복사가 없고 빠릅니다.

### ② 드라이브 문자 변경 주의
- 외장을 뺐다 꽂으면 `F:` 가 `E:` 로 바뀔 수 있습니다. 색인·되돌리기 기록은 예전 문자로
  저장돼 있어, 문자가 바뀌면 되돌리기가 맞지 않습니다.
- **권장:** 필요한 되돌리기는 **장치를 분리하기 전에**, 드라이브 문자가 같을 때 끝내세요.

### ③ 휴지통 모드는 외장에서 불안정
- 일부 외장·USB·FAT/exFAT·NAS 에는 휴지통($Recycle.Bin)이 없어 동작이 보장되지 않습니다.
- **권장:** 외장 정리는 **휴지통 토글을 끄고 격리폴더 모드**(+ "같은 드라이브에 격리")로 하세요.

### ④ 작업 중 장치를 분리하지 마세요
- 중간에 분리되면 나머지 파일이 안 옮겨질 뿐 소실은 없습니다. 다시 연결 후 재실행하면 됩니다.

### ⑤ 네트워크 드라이브(NAS 등)
- 권한·긴 경로·속도 문제로 느릴 수 있습니다. 오류는 건너뛰고 로그에 남습니다.

---

## 3. 권장 정리 순서 (외장 포함)

1. GUI에서 **검사할 폴더**에 대상 드라이브/폴더만 추가
2. 외장이면 **"같은 드라이브에 격리" 켜기**, **"휴지통" 끄기**
3. **① 스캔 및 분석**
4. 완전중복부터 확인(자동 체크됨) → **② 미리보기**
5. 이상 없으면 **③ 실제 이동**
6. 결과 확인 후 문제 없으면 격리폴더(`_정리보관`)를 나중에 직접 비우기
7. 잘못 옮겼으면 **되돌리기** (드라이브 문자가 같은 동안)
